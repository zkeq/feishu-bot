# 饮食分析机器人 - 日期识别功能说明

## 功能概述

现在饮食分析机器人支持识别用户消息中提及的日期，可以将饮食记录保存到指定日期，而不仅仅是当前日期。

## 功能特性

### 1. 智能日期识别

AI 会自动识别用户消息中的日期表达，包括但不限于：
- **明确日期**："1月20日"、"2026-01-20"
- **相对日期**："昨天"、"前天"、"上周五"
- **星期表达**："周一"、"上个周三"
- **当天**：如果没有提及日期，默认使用当天

### 2. 上下文感知

在提示词中，AI 会收到：
- 当前日期（YYYY-MM-DD 格式）
- 当前是星期几
- 用户的日期表达

这样 AI 可以准确计算出用户想要记录的日期。

## 使用示例

### 示例 1：记录昨天的饮食

**用户输入**：
```
昨天中午吃的麻辣香锅
```

**AI 识别**：
- 识别到"昨天"
- 根据今天是 2026-01-25，计算出昨天是 2026-01-24
- 在 JSON 数据中返回：`"date": "2026-01-24"`

**保存到多维表格**：
- 日期字段：2026-01-24
- 其他字段：餐次、热量等

### 示例 2：记录上周五的饮食

**用户输入**：
```
上周五晚上吃的火锅，热量爆表了
```

**AI 识别**：
- 识别到"上周五"
- 根据今天是 2026-01-25 周日，计算出上周五是 2026-01-17
- 在 JSON 数据中返回：`"date": "2026-01-17"`

### 示例 3：记录明确日期的饮食

**用户输入**：
```
1月20日的早餐，豆浆油条
```

**AI 识别**：
- 识别到"1月20日"
- 转换为：2026-01-20
- 在 JSON 数据中返回：`"date": "2026-01-20"`

### 示例 4：记录今天的饮食（默认）

**用户输入**：
```
今天午餐吃的沙拉
```

或者不提及日期：
```
午餐吃的沙拉
```

**AI 识别**：
- 没有特殊日期表达，使用当前日期
- 在 JSON 数据中返回：`"date": "2026-01-25"`

## 技术实现

### 1. 提示词增强

在 bot.py 的 `_build_ai_messages()` 方法中，自动添加当前日期信息：

```python
# 获取当前日期信息
now = datetime.now()
current_date = now.strftime("%Y-%m-%d")
weekday_names = ["周一", "周二", "周三", "周四", "周五", "周六", "周日"]
current_weekday = weekday_names[now.weekday()]

# 将日期信息添加到系统提示词
system_prompt_with_date = f"{self.get_system_prompt()}\n\n**当前时间信息**：今天是 {current_date} {current_weekday}。用户可能会提及日期（如'上周五'、'1月20日'等），请根据当前日期计算并在 JSON 的 date 字段中返回对应的 YYYY-MM-DD 格式日期。如果用户没有指定日期，则使用当前日期。"
```

### 2. JSON 格式更新

在 config.yaml 中，JSON 格式增加了 `date` 字段：

```json
{
  "date": "YYYY-MM-DD格式的日期",
  "meal_type": "早餐/午餐/晚餐/加餐",
  "main_dish": "主餐食物名称",
  ...
}
```

### 3. 数据保存

在 bot.py 的 `_save_to_bitable()` 方法中，将日期转换为飞书多维表格所需的时间戳格式：

```python
if "date" in meal_data and "date" in fields_mapping:
    # 将日期转换为时间戳（毫秒）
    date_obj = datetime.strptime(meal_data["date"], "%Y-%m-%d")
    timestamp_ms = int(date_obj.timestamp() * 1000)
    record_fields[fields_mapping["date"]] = timestamp_ms
```

### 4. 多维表格配置

在 config.yaml 的字段映射中添加了日期字段：

```yaml
bitable:
  fields:
    date: "日期"  # 对应多维表格中的"日期"字段
    ...
```

## 配置要求

### 飞书多维表格字段

确保你的飞书多维表格中有以下字段：

| 字段名称 | 字段类型 | 说明 |
|---------|---------|------|
| 日期 | 日期 | 用于存储饮食记录的日期 |
| 类型 | 单选 | 早餐/午餐/晚餐/加餐 |
| 主餐 | 文本 | 主餐内容 |
| 热量（kcal） | 数字 | 总热量 |
| ... | ... | 其他字段 |

**重要**：
- "日期"字段必须是飞书的**日期类型**
- 字段名称必须与 config.yaml 中的映射完全匹配

### config.yaml 配置

```yaml
bitable:
  enabled: true
  app_token: "你的多维表格 app_token"
  table_id: "你的数据表 table_id"
  fields:
    date: "日期"  # 必须添加这一行
    meal_type: "类型"
    # ... 其他字段映射
```

## 使用提示

### 1. 自然语言输入

鼓励用户使用自然语言描述日期：
- ✅ "昨天吃的"
- ✅ "上周五的晚餐"
- ✅ "1月20日中午"
- ✅ "前天早餐"

### 2. 日期计算准确性

由于使用 AI 进行日期识别和计算：
- 大部分常见日期表达都能准确识别
- 如果 AI 计算错误，可以在飞书多维表格中手动修改
- 建议用户在发送消息时明确提及日期

### 3. 时区注意事项

- 系统使用服务器本地时间作为基准
- 如果服务器和用户在不同时区，可能会有时差问题
- 建议确保服务器时区设置正确

### 4. 多日期补录

如果需要补录多天的饮食记录：
- 每条消息单独发送（一条消息对应一次饮食记录）
- 在每条消息中明确说明日期
- 例如：
  - 消息1："1月20日早餐..."
  - 消息2："1月20日午餐..."
  - 消息3："1月21日早餐..."

## 常见问题

### Q1：AI 识别的日期不准确怎么办？

**A**：可以在飞书多维表格中手动修改日期字段。如果发现某类日期表达经常出错，可以调整提示词以提高准确性。

### Q2：如何记录跨月份的日期？

**A**：明确说明月份和日期，例如：
- "12月31日的晚餐"
- "上个月20号的午餐"

### Q3：如果多维表格中没有"日期"字段会怎样？

**A**：代码会跳过日期字段，不会影响其他数据的保存。但建议添加日期字段以充分利用此功能。

### Q4：支持未来日期吗？

**A**：技术上支持，AI 可以识别"明天"、"下周一"等未来日期表达。但建议主要用于记录历史饮食。

### Q5：日期格式是什么？

**A**：
- AI 返回的格式：YYYY-MM-DD（如 2026-01-25）
- 存储到飞书的格式：Unix 时间戳（毫秒）
- 飞书多维表格中显示：根据表格设置的日期格式

## 示例对话

### 完整对话示例

**用户**：
```
上周五晚上吃的麻辣香锅，加了很多肉和蔬菜
```

**AI 分析**：
```markdown
**饮食分析** 🍜

哇！麻辣香锅听起来超级诱人呀！让我帮你分析一下上周五（1月17日）这顿晚餐的营养成分：

• 麻辣香锅（肉类+蔬菜）：约 1200 kcal
• 估计蛋白质：45g
• 估计碳水：60g
• 估计脂肪：55g

**营养建议** 💡

这是一顿热量较高的餐食，但营养比较均衡...

```json
{
  "date": "2026-01-17",
  "meal_type": "晚餐",
  "main_dish": "麻辣香锅",
  "snacks": "",
  "drinks": "",
  "calories": 1200,
  "protein": 45,
  "carbs": 60,
  "fat": 55,
  "score": 7,
  "notes": "麻辣香锅，肉菜搭配"
}
```
```

**用户点击**：📥 导入到多维表格

**结果**：
- ✅ 数据成功保存到 2026-01-17 的记录中

## 总结

通过添加日期识别功能，饮食分析机器人现在可以：
- ✅ 识别用户提及的各种日期表达
- ✅ 自动计算相对日期（昨天、上周五等）
- ✅ 将饮食记录保存到正确的日期
- ✅ 支持补录历史饮食记录
- ✅ 提供更灵活的饮食记录方式

这样你就可以随时补录之前忘记记录的饮食啦！🎉
