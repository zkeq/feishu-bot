# 饮食分析机器人 - 日期识别功能说明

## 功能概述

现在饮食分析机器人支持识别用户消息中提及的日期，可以将饮食记录保存到指定日期，而不仅仅是当前日期。

## 功能特性

### 1. 智能日期识别（三层优先级）

AI 会按照以下优先级识别日期：

**优先级 1：图片时间水印（最高优先级）**
- 如果图片左下角有时间水印，AI 会优先读取并使用该时间
- 支持的时间格式：
  - "2026-01-20 12:30"
  - "2026/01/20 12:30"
  - "2026.01.20 12:30"
  - 其他常见格式
- AI 只提取日期部分（YYYY-MM-DD），忽略具体时间

**优先级 2：用户文字描述中的日期**
- 如果没有图片时间水印，AI 会识别用户消息中的日期表达
- 支持的表达：
  - **明确日期**："1月20日"、"2026-01-20"
  - **相对日期**："昨天"、"前天"、"上周五"
  - **星期表达**："周一"、"上个周三"

**优先级 3：当前日期（默认）**
- 如果既没有图片时间也没有文字提及日期，使用当天日期

### 2. 上下文感知

在提示词中，AI 会收到：
- 当前日期（YYYY-MM-DD 格式）
- 当前是星期几
- 用户的日期表达

这样 AI 可以准确计算出用户想要记录的日期。

## 使用示例

### 示例 1：使用图片时间水印（优先级最高）

**用户输入**：
```
[上传一张图片，左下角有水印：2026-01-20 12:30]
今天中午吃的麻辣香锅
```

**AI 识别**：
- 读取图片左下角的时间水印：2026-01-20 12:30
- 提取日期部分：2026-01-20
- **忽略用户说的"今天"**，优先使用图片时间
- 在 JSON 数据中返回：`"date": "2026-01-20"`

**保存到多维表格**：
- 日期字段：2026-01-20（图片拍摄日期）
- 其他字段：餐次、热量等

**说明**：即使用户说"今天"，但图片时间显示是 1月20日，AI 会使用图片时间（1月20日）而不是今天的日期。

### 示例 2：没有图片，使用文字描述中的日期

**用户输入**：
```
昨天中午吃的麻辣香锅
```

**AI 识别**：
- 没有图片或图片没有时间水印
- 识别到"昨天"
- 根据今天是 2026-01-25，计算出昨天是 2026-01-24
- 在 JSON 数据中返回：`"date": "2026-01-24"`

**保存到多维表格**：
- 日期字段：2026-01-24
- 其他字段：餐次、热量等

### 示例 3：图片和文字都有日期信息

**用户输入**：
```
[上传一张图片，左下角有水印：2026-01-18 18:30]
这是上周五晚上吃的火锅
```

**AI 识别**：
- 读取图片时间水印：2026-01-18
- 用户提到"上周五"（实际是 2026-01-17）
- **图片时间优先**，使用 2026-01-18
- 在 JSON 数据中返回：`"date": "2026-01-18"`

**说明**：图片时间和文字描述略有差异时，以图片时间为准。

### 示例 4：记录明确日期的饮食

**用户输入**：
```
1月20日的早餐，豆浆油条
```

**AI 识别**：
- 识别到"1月20日"
- 转换为：2026-01-20
- 在 JSON 数据中返回：`"date": "2026-01-20"`

### 示例 5：记录今天的饮食（默认）

**用户输入**：
```
今天午餐吃的沙拉
```

或者不提及日期：
```
午餐吃的沙拉
```

**AI 识别**：
- 没有图片时间水印
- 没有特殊日期表达，使用当前日期
- 在 JSON 数据中返回：`"date": "2026-01-25"`

## 技术实现

### 1. 提示词增强

在 bot.py 的 `_build_ai_messages()` 方法中，自动添加当前日期信息：

```python
# 获取当前日期信息
now = datetime.now()
current_date = now.strftime("%Y-%m-%d")
weekday_names = ["周一", "周二", "周三", "周四", "周五", "周六", "周日"]
current_weekday = weekday_names[now.weekday()]

# 将日期信息添加到系统提示词
system_prompt_with_date = f"{self.get_system_prompt()}\n\n**当前时间信息**：今天是 {current_date} {current_weekday}。用户可能会提及日期（如'上周五'、'1月20日'等），请根据当前日期计算并在 JSON 的 date 字段中返回对应的 YYYY-MM-DD 格式日期。如果用户没有指定日期，则使用当前日期。"
```

### 2. JSON 格式更新

在 config.yaml 中，JSON 格式增加了 `date` 字段：

```json
{
  "date": "YYYY-MM-DD格式的日期",
  "meal_type": "早餐/午餐/晚餐/加餐",
  "main_dish": "主餐食物名称",
  ...
}
```

### 3. 数据保存

在 bot.py 的 `_save_to_bitable()` 方法中，将日期转换为飞书多维表格所需的时间戳格式：

```python
if "date" in meal_data and "date" in fields_mapping:
    # 将日期转换为时间戳（毫秒）
    date_obj = datetime.strptime(meal_data["date"], "%Y-%m-%d")
    timestamp_ms = int(date_obj.timestamp() * 1000)
    record_fields[fields_mapping["date"]] = timestamp_ms
```

### 4. 多维表格配置

在 config.yaml 的字段映射中添加了日期字段：

```yaml
bitable:
  fields:
    date: "日期"  # 对应多维表格中的"日期"字段
    ...
```

## 配置要求

### 飞书多维表格字段

确保你的飞书多维表格中有以下字段：

| 字段名称 | 字段类型 | 说明 |
|---------|---------|------|
| 日期 | 日期 | 用于存储饮食记录的日期 |
| 类型 | 单选 | 早餐/午餐/晚餐/加餐 |
| 主餐 | 文本 | 主餐内容 |
| 热量（kcal） | 数字 | 总热量 |
| ... | ... | 其他字段 |

**重要**：
- "日期"字段必须是飞书的**日期类型**
- 字段名称必须与 config.yaml 中的映射完全匹配

### config.yaml 配置

```yaml
bitable:
  enabled: true
  app_token: "你的多维表格 app_token"
  table_id: "你的数据表 table_id"
  fields:
    date: "日期"  # 必须添加这一行
    meal_type: "类型"
    # ... 其他字段映射
```

## 使用提示

### 1. 图片时间水印识别

**优势**：
- ✅ 最准确：使用照片的实际拍摄时间
- ✅ 自动识别：无需手动输入日期
- ✅ 适合补录：拍照时就记录了准确时间

**注意事项**：
- 确保图片左下角的时间水印清晰可见
- 如果图片经过裁剪可能丢失时间水印
- AI 会自动提取日期，忽略具体时间（时:分:秒）

### 2. 自然语言输入

当没有图片或图片没有时间水印时，可以使用自然语言：
- ✅ "昨天吃的"
- ✅ "上周五的晚餐"
- ✅ "1月20日中午"
- ✅ "前天早餐"

### 3. 日期优先级说明

```
图片时间水印（最高优先级）
       ↓ 如果没有
用户文字中的日期描述
       ↓ 如果没有
当前日期（默认）
```

**重要**：当图片有时间水印时，即使用户文字中提到其他日期，也会优先使用图片时间。

### 2. 日期计算准确性

**图片时间水印**：
- 准确度：非常高（直接读取图片元数据）
- AI 会尽力识别各种时间格式
- 如果水印不清晰，可能识别失败，此时会使用文字描述

**文字日期描述**：
- 大部分常见日期表达都能准确识别
- 如果 AI 计算错误，可以在飞书多维表格中手动修改
- 建议用户在发送消息时明确提及日期

### 3. 时区注意事项

- 系统使用服务器本地时间作为基准
- 如果服务器和用户在不同时区，可能会有时差问题
- 建议确保服务器时区设置正确

### 4. 多日期补录

如果需要补录多天的饮食记录：
- 每条消息单独发送（一条消息对应一次饮食记录）
- 在每条消息中明确说明日期
- 例如：
  - 消息1："1月20日早餐..."
  - 消息2："1月20日午餐..."
  - 消息3："1月21日早餐..."

## 常见问题

### Q1：图片有时间水印但 AI 没有识别到怎么办？

**A**：
1. 确保时间水印在图片左下角且清晰可见
2. 如果图片经过编辑或裁剪，时间水印可能被移除
3. AI 可能无法识别某些特殊格式的水印
4. 如果识别失败，AI 会退回使用文字描述中的日期
5. 最后可以在飞书多维表格中手动修改

### Q2：图片时间和文字描述不一致怎么办？

**A**：系统会优先使用图片时间。例如：
- 图片时间：2026-01-18
- 文字说："昨天吃的"（假设今天是 2026-01-25）
- 结果：使用 2026-01-18（图片时间）

如果图片时间不准确（比如手机时间设置错误），建议：
- 方案1：删除或裁剪掉时间水印后重新上传
- 方案2：在多维表格中手动修改日期

### Q3：AI 识别的日期不准确怎么办？

**A**：可以在飞书多维表格中手动修改日期字段。如果发现某类日期表达经常出错，可以调整提示词以提高准确性。

### Q4：如何记录跨月份的日期？

**A**：
- 如果有图片时间水印，无需额外操作，AI 会自动识别
- 如果没有图片，明确说明月份和日期，例如：
  - "12月31日的晚餐"
  - "上个月20号的午餐"

### Q5：如果多维表格中没有"日期"字段会怎样？

**A**：代码会跳过日期字段，不会影响其他数据的保存。但建议添加日期字段以充分利用此功能。

### Q6：支持未来日期吗？

**A**：技术上支持，AI 可以识别"明天"、"下周一"等未来日期表达。但建议主要用于记录历史饮食。

### Q7：图片时间水印的格式有要求吗？

**A**：AI 支持多种常见格式：
- "2026-01-20 12:30"
- "2026/01/20 12:30"
- "2026.01.20 12:30"
- "2026年01月20日 12:30"
- 其他类似格式

AI 会自动识别并提取日期部分。

### Q8：日期格式是什么？

**A**：
- AI 返回的格式：YYYY-MM-DD（如 2026-01-25）
- 存储到飞书的格式：Unix 时间戳（毫秒）
- 飞书多维表格中显示：根据表格设置的日期格式

## 示例对话

### 完整对话示例 1：使用图片时间水印

**用户**：
```
[上传一张图片，左下角显示：2026-01-18 18:30]
这是我晚上吃的麻辣香锅，加了很多肉和蔬菜
```

**AI 分析**：
```markdown
**饮食分析** 🍜

从图片可以看到，这是 **2026年1月18日** 拍摄的照片哦！

哇！麻辣香锅看起来超级诱人呀！让我帮你分析一下这顿晚餐的营养成分：

• 麻辣香锅（肉类+蔬菜）：约 1200 kcal
• 估计蛋白质：45g
• 估计碳水：60g
• 估计脂肪：55g

**营养建议** 💡

这是一顿热量较高的餐食，但营养比较均衡...

```json
{
  "date": "2026-01-18",
  "meal_type": "晚餐",
  "main_dish": "麻辣香锅",
  "snacks": "",
  "drinks": "",
  "calories": 1200,
  "protein": 45,
  "carbs": 60,
  "fat": 55,
  "score": 7,
  "notes": "麻辣香锅，肉菜搭配，图片拍摄于1月18日"
}
```
```

**结果**：
- ✅ 使用图片时间：2026-01-18（不是今天）
- ✅ 数据成功保存到 2026-01-18 的记录中

### 完整对话示例 2：没有图片，使用文字日期

**用户**：
```
上周五晚上吃的麻辣香锅，加了很多肉和蔬菜
```

**AI 分析**：
```markdown
**饮食分析** 🍜

哇！麻辣香锅听起来超级诱人呀！让我帮你分析一下上周五（1月17日）这顿晚餐的营养成分：

• 麻辣香锅（肉类+蔬菜）：约 1200 kcal
• 估计蛋白质：45g
• 估计碳水：60g
• 估计脂肪：55g

**营养建议** 💡

这是一顿热量较高的餐食，但营养比较均衡...

```json
{
  "date": "2026-01-17",
  "meal_type": "晚餐",
  "main_dish": "麻辣香锅",
  "snacks": "",
  "drinks": "",
  "calories": 1200,
  "protein": 45,
  "carbs": 60,
  "fat": 55,
  "score": 7,
  "notes": "麻辣香锅，肉菜搭配"
}
```
```

**用户点击**：📥 导入到多维表格

**结果**：
- ✅ 数据成功保存到 2026-01-17 的记录中

## 总结

通过添加图片时间水印识别功能，饮食分析机器人现在可以：
- ✅ **优先识别图片左下角的时间水印**（最准确）
- ✅ 其次识别用户文字中的日期表达
- ✅ 自动计算相对日期（昨天、上周五等）
- ✅ 将饮食记录保存到正确的日期
- ✅ 支持补录历史饮食记录
- ✅ 提供更灵活、更准确的饮食记录方式

**三层日期识别优先级**：
```
1. 图片时间水印（最高优先级）
   ↓ 如果没有
2. 用户文字描述中的日期
   ↓ 如果没有
3. 当前日期（默认）
```

这样你就可以随时补录之前忘记记录的饮食，而且日期会非常准确（使用照片的实际拍摄时间）！🎉
